# Snapdragon NPU OpenClaw Demo

Keep prompts short. Use the configured local model provider only. This workspace exists to keep OpenClaw's startup context small enough for NPU-backed Foundry Local demos.
